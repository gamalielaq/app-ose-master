export interface Months {
    name: string;
    code: string;
}
export interface Period {
    name: string;
    code: string;
}
export interface Year {
    name: string;
    code: string;
}
export interface Filter {
    name: string;
    code: string;
}